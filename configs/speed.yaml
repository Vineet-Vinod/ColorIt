model:
  repo_id: spensercai/DeOldify
  filename: ColorizeVideo_gen.pth
  weights_path: models/deoldify/ColorizeVideo_gen.pth
  render_factor: 17

runtime:
  backend_preference: mps
  fallback_backend: cpu
  num_workers: 1
  deterministic: true
  frame_transport: pipe
  cleanup_frames: true

video:
  preserve_fps: true
  output_codec: libx264
  crf: 18
  pixel_format: yuv420p

compression:
  review_video_codec: libx264
  review_preset: slow
  review_crf: 22
  review_audio_codec: aac
  review_audio_bitrate: 128k
  review_faststart: true

scenes:
  detect: true
  min_scene_seconds: 2.0
  max_scene_seconds: 90.0
  overlap_seconds: 1.0

postprocess:
  temporal_smoothing: false
  smoothing_strength: 0.0
  smoothing_chroma_threshold: 24.0
  adaptive_smoothing_boost: 0.0
  warmth: 0.0
  shadow_warmth: 0.0
  blue_reduction: 0.0

paths:
  source_dir: data/source
  probe_dir: data/probe_clips
  scene_dir: data/scenes
  colorized_dir: data/colorized
  manifest_dir: data/manifests
  final_dir: data/final
  log_dir: data/logs
